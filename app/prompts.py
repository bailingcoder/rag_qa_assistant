SYSTEM_PROMPT_WITH_CONTEXT = (
    "你是一个知识库问答助手。请严格根据下面提供的资料回答问题。"
    "如果资料中没有答案，就回答'抱歉，资料中没有相关信息'，不要编造。\n\n"
    "资料: {context}\n"
)

SYSTEM_PROMPT_NO_CONTEXT = (
    "你是一个友好的助手。当前问题不需要检索资料，请直接简洁地回答。"
    "资料: {context}\n"
)


ROUTE_PROMPT = (
    "你是一个路由器，判断用户问题是否需要检索知识库。\n"
    "分类规则：\n"
    "1. 寒暄、问候、闲聊（如'你好''谢谢''再见'）→ 不需要检索\n"
    "2. 与知识库主题无关的通用问题（如写诗、翻译、天气、算数）→ 不需要检索\n"
    "3. 需要基于知识库文档才能回答的专业问题 → 需要检索\n\n"
    "4.给出你这样判断的理由\n\n"
    "问题：{question}\n\n"
    "只输出一个 JSON 对象，不要任何其他文字，格式如下：\n"
    '{{"need_retrieve": false, "reason": "这是寒暄，不需要检索"}}'
)

REWRITE_PROMPT = """
你是一个对话查询重写助手。根据对话历史和用户当前的问题，
把用户的问题改写成一个独立的、完整的问句，用于后续的向量检索。
要求：
1. 把代词（它、这个、那个、这些等）替换成它们具体指代的内容
2. 把省略的主语、宾语、背景信息补全
3. 只输出改写后的完整问句本身，不要任何解释、前缀、引号或代码块
4. 如果当前问题本身已经完整、不依赖历史，就原样输出

对话历史：
{history}

用户当前问题：{question}

改写后的完整问题：
"""

CLARIFY_PROMPT = """你是一个问答助手。判断下面这个问题信息是否足够、可以直接检索回答，还是需要向用户反问澄清。

判断标准：
- 如果问题里的指代（它、这个、那个）、实体、范围等信息不明确，且无法从对话历史推断，就需要反问
- 如果问题本身清晰完整，不需要澄清

请只输出一个 JSON 对象，格式如下：
{{"clarify_question": ""}}

如果问题信息不足需要反问，clarify_question 填你要反问用户的问题（一句话，具体指出缺什么信息）；如果不需要澄清，clarify_question 留空字符串。

对话历史：
{history}

用户问题：{question}
"""